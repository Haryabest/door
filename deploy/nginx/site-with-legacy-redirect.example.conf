# Пример nginx: основной домен + редирект со старого хоста.
# Переименуйте домены и скопируйте в sites-enabled. Не включается автоматически.

# ── Редирект со старого имени хоста (если нужно сохранить выдачу) ─────────
# Раскомментируйте и подставьте реальные имена сертификатов / пути SSL.
#
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name old-example.ru www.old-example.ru;
#
#     # ssl_certificate     /etc/letsencrypt/live/old-example.ru/fullchain.pem;
#     # ssl_certificate_key /etc/letsencrypt/live/old-example.ru/privkey.pem;
#
#     return 301 https://НОВЫЙ_ДОМЕН.ru$request_uri;
# }

# ── Основной сайт (статический билд SPA + опционально прокси на API) ─────────
#
# upstream doors_api {
#     server 127.0.0.1:3001;
#     keepalive 32;
# }
#
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name НОВЫЙ_ДОМЕН.ru www.НОВЫЙ_ДОМЕН.ru;
#
#     # ssl_certificate     /etc/letsencrypt/live/НОВЫЙ_ДОМЕН.ru/fullchain.pem;
#     # ssl_certificate_key /etc/letsencrypt/live/НОВЫЙ_ДОМЕН.ru/privkey.pem;
#
#     add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
#     add_header X-Frame-Options "DENY" always;
#     add_header X-Content-Type-Options "nosniff" always;
#     add_header Referrer-Policy "strict-origin-when-cross-origin" always;
#
#     root /var/www/doors/dist;
#     index index.html;
#
#     location /api/ {
#         proxy_pass http://doors_api;
#         proxy_http_version 1.1;
#         proxy_set_header Host              $host;
#         proxy_set_header X-Real-IP         $remote_addr;
#         proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
#
#     location /uploads/ {
#         proxy_pass http://doors_api;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#     }
#
#     location / {
#         try_files $uri $uri/ /index.html;
#     }
#
#     location = /robots.txt  { access_log off; }
#     location = /sitemap.xml { access_log off; }
# }
#
# server {
#     listen 80;
#     listen [::]:80;
#     server_name НОВЫЙ_ДОМЕН.ru www.НОВЫЙ_ДОМЕН.ru;
#     return 301 https://НОВЫЙ_ДОМЕН.ru$request_uri;
# }
